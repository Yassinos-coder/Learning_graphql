import { StyleSheet } from "react-native";

const GateStyle = StyleSheet.create({
    container: {
        flex: 1,
        width: 500,

    },
    inputs: {
        
    }
})

export default GateStyle